// 类型注解
// 类型推断
const one = 1;
const two = 2;
const three = one + two

function getTotal (one:number,two:number){
  return one+two
}
const total = getTotal(1,2)
const xiaoJieJie = {
  name:'刘颖',
  age:18
}